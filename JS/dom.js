const bar = document.querySelector("#bar path");
const barPath = document.querySelector("#path");
const circle = document.querySelectorAll(".circle");
const imageComponent = document.querySelectorAll(
  ".slide__imgwrapper__component"
);
const layerOnTOp = document.querySelector(".layerOnTop");
const container = document.querySelector(".container");
const dots = document.querySelectorAll(".dot");
const dotsWrapper = document.querySelectorAll(".dot__wrapper");
const slides = document.querySelectorAll(".slide");

const imageComponentWrapper = document.querySelectorAll(".slide__imgwrapper");
export {
  bar,
  barPath,
  circle,
  imageComponent,
  imageComponentWrapper,
  layerOnTOp,
  container,
  dots,
  dotsWrapper,
  slides,
};
